import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
     button: {
    flexDirection: 'row',
    alignItems: 'center',
    borderWidth: 1,
    borderColor: '#555',
    borderRadius: 50,
    paddingVertical: 14,
    paddingHorizontal: 20,
    width: '100%',
    marginBottom: 12,
  },
  icon: {
    width: 20,
    height: 20,
    marginRight: 16,
  },
  label: {
    color: '#fff',
    fontWeight: 'bold',
    fontSize: 15,
  },
});